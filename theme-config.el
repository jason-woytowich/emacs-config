;;; theme-config --- Theme and UI related configuration

;;; Commentary:

;;; Code:
(require 'atom-dark-theme)

(line-number-mode t)
(column-number-mode t)
(size-indication-mode t)
(scroll-bar-mode 0)
(tool-bar-mode 0)

(provide 'theme-config)
;;; theme-config.el ends here
